services:
  paper-quant:
    build: .
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data
    environment:
      DRY_RUN: "true"
      PAPER_TRADING_ONLY: "true"
