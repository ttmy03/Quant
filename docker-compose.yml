services:
  paper-quant:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      DRY_RUN: "true"
      PAPER_TRADING_ONLY: "true"
